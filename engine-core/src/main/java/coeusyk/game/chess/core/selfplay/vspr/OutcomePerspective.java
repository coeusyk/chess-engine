package coeusyk.game.chess.core.selfplay.vspr;

/** Wire values are this enum's declaration order (DR-220 section 6) -- never reorder. */
public enum OutcomePerspective {
    WHITE,
    SIDE_TO_MOVE_AT_SAMPLE
}
