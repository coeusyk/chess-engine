package coeusyk.game.chess.core.selfplay.vspr;

/** Wire values are this enum's declaration order (DR-220 section 5) -- never reorder. */
public enum SelectionMechanismKind {
    BEST_MOVE,
    NAMED
}
