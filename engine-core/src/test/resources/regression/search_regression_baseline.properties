# Search regression baseline — maintained by SearchRegressionSuite.java
# Update: mvn test -pl engine-core -Psearch-regression -Dupdate-baseline=true
#
# wac.pass.rate     = fraction of WAC EPD positions solved correctly at PASS_DEPTH
# wac.flip.rate.max = maximum allowed depth-stability flip rate (D_LOW vs D_HIGH)
wac.pass.rate=0.80
wac.flip.rate.max=0.35
