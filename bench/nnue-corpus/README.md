# NNUE Benchmark Corpus

PR C-4 (issue #188). Six category files + a golden-eval pin, used for
per-category eval throughput / NPS tracking (`NnueCorpusBenchmarkTest`) and a
hard-failure regression check against the CI test net (`NnueGoldenEvalTest`).
Not wired into CI yet — that's PR C-5.

## Categories

| File | Source | Curation |
|---|---|---|
| `tactical.epd` | Direct copy of `engine-core/src/test/resources/tactical/mate_2_3_50.epd` | None |
| `quiet.epd` | Deterministic subset of `tools/quiet-labeled.epd` (gitignored — this file is a standalone copy, not a reference) | Every 7250th line (`NR % 7250 == 1`), 100 FENs |
| `opening.epd` | Fixed-seed (`20260713`) random-legal self-play, ply 1-15 | Generated |
| `middlegame.epd` | Same self-play run, ply 16-40 | Generated |
| `endgame.epd` | Self-play tail (<= 12 pieces on board) + all 3 curated positions from `engine-core/src/test/resources/regression/draw_failures.epd` | Generated + folded |
| `random-legal.epd` | Same self-play run, uniformly sampled across full-length games (any ply) | Generated |
| `golden-evals.csv` | `fen,eval` — every FEN above, evaluated once by the CI test net (`TestNetworks.synthetic(8)`) | Generated |

Each `.epd` line is a 4-field FEN (`<board> <side> <castling> <ep>`), matching
this repo's existing `.epd` convention (see `engine-core/src/test/resources/regression/wac.epd`).
`Board(String)` requires the 6-field form — callers append `" 0 1"` before
constructing (`NnueCorpusCategories.toBoard`).

## Regenerating

The generator (`NnueCorpusGenerator`, `engine-core/src/test/java/.../eval/nnue/`)
is excluded from the standard test suite and from CI. It is meant to be run
manually, and its *output* — not the generator itself — is what's committed.
Re-running it is expected to be byte-identical (verified: two consecutive
runs produced identical files).

```
mvn -pl engine-core test -Dgroups=corpus-generation -Dcorpus.generate=true -Dtest=NnueCorpusGenerator
```

This regenerates all four self-play-derived `.epd` files and `golden-evals.csv`
in one pass (`generateCorpus()` calls category generation, then golden-eval
generation, in that order — JUnit does not guarantee method execution order
within a class, hence the single-method design). `tactical.epd` and
`quiet.epd` are not regenerated by this command — they're static copies;
re-copy them by hand from their sources above if they ever need refreshing.

If the CI test net's weights ever change, `golden-evals.csv` must be
regenerated (never hand-edited) — the expected values are net-specific.

## Running the benchmark harness

```
mvn -pl engine-core test -Dgroups=nnue-benchmark -Dbenchmark.enabled=true -Dtest=NnueCorpusBenchmarkTest
```

Deliberately its own tag (`nnue-benchmark`), distinct from `NpsBenchmarkTest`'s
`benchmark` tag — `ci.yml`'s existing `-Dgroups=benchmark` step has no
`-Dtest=` scoping, so sharing that tag would have made this ride along on
every CI run despite the "not wired into CI yet" intent below.

Reports per-category eval throughput (evals/sec) and fixed-depth NPS
(depth 6 by default, `-Dbenchmark.depth=N` to override) for both the NNUE
and classical evaluators. These figures are *tracked*, not gated — the
single aggregate NPS number in `NpsBenchmarkTest` / CLAUDE.md §3 remains the
only enforced gate (plan Task 3, "Regression thresholds").

## Running the golden-eval check

```
mvn -pl engine-core test -Dtest=NnueGoldenEvalTest
```

Runs as part of the standard `mvn test` reactor build (no flag needed) —
any mismatch is a hard failure, not a threshold.
